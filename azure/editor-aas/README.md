## "Editor as a Service": Azure Ubuntu VM with Visual Studio Code and a Git repository preinstalled

The user has to connect via ssh to change his password before being able to use the gui with a Remote Desktop Client and RDP